const request = require("supertest");
const app = require("../../server");

describe("integration test cases", () => {
    test("bypass the integration test", async () => {
        expect(1).toBe(1);
    });

    // add some test cases
});
