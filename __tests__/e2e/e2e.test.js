const request = require("supertest");
const app = require("../../server");

describe("end-to-end test cases", () => {
    test("bypass the e2e test", async () => {
        expect(1).toBe(1);
    });

    // add some test cases
});
